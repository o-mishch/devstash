# Feature History

History is ordered oldest to newest. New entries are appended to the END.

- **Initial Setup** - Next.js 16, Tailwind CSS v4, TypeScript configured (Completed)
- **Dashboard UI Phase 1** - ShadCN UI init, `/dashboard` route, dark mode, top bar with search + buttons, sidebar/main placeholders (Completed)
- **Dashboard UI Phase 2** - Collapsible sidebar with icon-only mode, item type links, collections with favorites/recent, user area, mobile Sheet drawer, app icon in header (Completed)
- **Dashboard UI Phase 3** - Stats cards, recent collections grid, pinned/recent items list, shared types and constants, SSR layout with client component islands (Completed)
- **Prisma + Neon PostgreSQL Setup** - Prisma 7 ORM with Neon PostgreSQL, full schema with all models and NextAuth tables, initial migration, system item types seeded, test-db script, legacy types replaced with Prisma-generated types (Completed)
- **Seed Data** - Demo user (`demo@devstash.one`), all 7 system item types, and 5 collections with realistic items: React Patterns (3 snippets), AI Workflows (3 prompts), DevOps (1 snippet, 1 command, 2 links), Terminal Commands (4 commands), Design Resources (4 links). Added `bcryptjs` for password hashing (Completed)
- **Dashboard Collections - Live Data** - `src/lib/db/collections.ts` with `getRecentCollections` and `getCurrentUserId`, collection cards with dominant-type left border color, type icons, item count from real DB. Items section still uses mock data (Completed)
- **Dashboard Items - Live Data** - `src/lib/db/items.ts` with `getPinnedItems`, `getRecentItems`, `getItemStats`; `getItemIcon` dynamic Lucide resolver from DB icon name; item rows show title, description, type icon, tags, date; Pinned section with Pin icon header hidden when empty; seed updated with pinned items; removed `lib/constants/item-types.ts` (Completed)
- **Stats & Sidebar Live Data** - `getCollectionStats` and `getItemTypeCounts` added; `DashboardLayout` made async to fetch sidebar data (collections + type counts); sidebar item types show live per-type counts; sidebar collections sourced from DB with colored circles for recents; "View all collections" link added; stats cards use real totals; `src/lib/mock-data.ts` deleted (Completed)
- **Pro Badge in Sidebar** - Subtle outline `Badge` (ShadCN UI) with "PRO" label added inline next to the Files and Images item types in the expanded sidebar; `PRO_TYPE_NAMES` set used for clean gating with no DB changes required (Completed)
- **Code Quality & Performance Pass** - Static lucide icon map, leaner `COLLECTION_INCLUDE` select, 5 missing DB indexes via migration, `formatDate`/`clampLimit` helpers in utils, DB query limit validation, dashboard route split into `layout.tsx`/`page.tsx` with `loading.tsx` skeleton and `error.tsx` boundary (Completed)
- **Auth Setup - NextAuth + GitHub Provider** - `next-auth@beta` + `@auth/prisma-adapter` installed with npm overrides for Prisma 7; split config pattern (`auth.config.ts` edge-compatible, `auth.ts` with Prisma adapter + JWT); GitHub OAuth provider; `/dashboard` protected via `src/proxy.ts` (Next.js 16 proxy); `src/types/next-auth.d.ts` extends Session with `user.id`; default NextAuth sign-in page used (Completed)
- **Auth Credentials - Email/Password Provider** - Credentials provider added to NextAuth using split config pattern: edge-safe placeholder in `auth.config.ts`, real bcrypt validation in `auth.ts`; duplicate provider deduped by filtering `authConfig.providers`; `POST /api/auth/register` validates inputs, checks for existing email, hashes with bcryptjs (cost 12), creates user, returns `{ success, user }` (Completed)
- **Auth UI - Sign In, Register & Sign Out** - Custom `/sign-in` page (SSR + client island): email/password form with `useActionState`, discriminated union state (`idle | success | error`), Sign in/Sign up buttons side-by-side, GitHub OAuth at bottom; custom `/register` page: client-side validation, `toast.error` for all errors, `toast.success` before redirect; sidebar user area: real session data, `UserAvatar` (GitHub image or initials), dropdown with Profile + Sign out via `signOutAction`; Sonner toast notifications with `richColors` and `position="top-center"`; `auth.config.ts` explicit `AuthorizedParams` interface; `auth.ts` explicit types for jwt/session callbacks; `confirmPassword` removed from API (Completed)
- **Email Verification on Register** - Resend SDK integration; `src/lib/tokens.ts` generates 32-byte hex tokens stored in `verification_tokens` table (24h TTL); `src/lib/emails/verification.ts` sends HTML email and handles rate-limited resend (55-min window) returning boolean; `GET /verify-email` SSR page consumes token, sets `emailVerified`, redirects to `/sign-in?verified=1`; `POST /api/auth/resend-verification` API route; unverified sign-in blocked in `signInWithCredentials` action with yellow banner + resend button; GitHub OAuth users unaffected; dashboard layout refactored to use Suspense + `SidebarSkeleton`; sidebar data fetching extracted to `src/lib/db/sidebar.ts` (pure DB) + `src/lib/sidebar-data.ts` (auth + React cache) (Completed)
- **Email Verification Toggle Flag** - `DISABLE_EMAIL_VERIFICATION=true` env var bypasses verification in dev; when set, register marks `emailVerified = new Date()` and skips email send; sign-in and NextAuth `authorize` both skip the unverified guard; `VerificationResult` discriminated union (`sent | failed | skipped`) drives response and UI; `ApiResponse<T>` type standardises register endpoint shape; `sendRegistrationVerification` extracted to `src/lib/emails/verification.ts`; `icon-utils.ts` refactored to `icon-utils.tsx` with `ItemTypeIcon` component backed by private `renderItemIcon` utility, eliminating dynamic `<Icon />` lint errors across all callers (Completed)
- **Forgot Password** - `POST /api/auth/forgot-password` generates a 32-byte hex token stored in `verification_tokens` with `identifier = "password-reset:<email>"` (1h TTL), sends reset email via Resend; `POST /api/auth/reset-password` (Server Action `resetPasswordAction`) consumes token single-use, checks account has a password, hashes with bcryptjs cost 12; `GET /reset-password?token=...` validates token SSR and renders form or error card; no user enumeration — always `200 ok`; OAuth-only accounts blocked at token creation and action; `verify-email` page guards against password-reset token cross-contamination; `/forgot-password` has two distinct screens (no logo): form card and "Check your email" success card with `CircleCheck` icon; all auth screens wrapped in `Card`/`CardContent`; `AuthFormLayout`/`AuthStatusPage` refactored to use shadcn Card; `register-content.tsx`, `auth-logo.tsx`, `status-card.tsx` deleted; `.env.example` updated with all variables (Completed)
- **Profile Page** - `/profile` route protected via `auth.config.ts`; `(app)` route group shares layout with `/dashboard` to avoid sidebar remount on navigation; `src/lib/db/profile.ts` fetches user info (name, email, image, password presence, OAuth providers, join date) and usage stats (total items, total collections, per-type counts) in one `Promise.all`; "Account Information" card: avatar, name, account type label (Email/GitHub/etc. derived from DB providers), email + member-since fields with icons, Change Password + Delete Account buttons in one row; Change Password shown only for email users (hidden for OAuth-only); `changePasswordAction` verifies current password before updating; `deleteAccountAction` deletes user row (cascades), signs out, redirects to `/`; Delete Account requires confirmation dialog; "Usage Statistics" card: total items/collections blocks + 4-column by-type grid; shadcn Dialog added (`@base-ui/react`) (Completed)
- **Rate Limiting for Auth** - `src/lib/rate-limit.ts` with `rateLimitRoute` (API routes) and `rateLimitAction` (server actions) helpers backed by Upstash Redis + `@upstash/ratelimit` sliding window; `src/lib/auth-service.ts` extracts `registerUser`, `triggerPasswordReset`, `applyPasswordReset` to decouple business logic from rate limiting; login keyed by IP+email (5/15min), register/forgot-password keyed by IP (3/1h), reset-password keyed by IP (5/15min), resend-verification dual-guarded with IP-only broad bucket + IP+email tight bucket; fail-open on Upstash unavailability; 429 responses include `Retry-After` header and human-readable message surfaced via toast; unused `/api/auth/reset-password` route removed (server action is the only path); `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN` added to `.env.example` (Completed)
- **OAuth Account Linking** - `signIn` callback in `auth.ts` intercepts GitHub OAuth when a credentials-only account exists for the same email → stores OAuth payload in Upstash Redis key `pending-link:<token>` (15-min TTL, 32-byte hex token) → returns redirect to `/link-account?token=...`; `/link-account` SSR page validates token via `getPendingLink` and renders an error card on expiry or the password-confirmation form otherwise; `linkAccountAction` server action verifies password via bcrypt, idempotently creates the `Account` DB row, deletes the Redis key, then calls `signIn('credentials', { redirectTo: '/dashboard' })` which throws `NEXT_REDIRECT` directly to the dashboard; `src/lib/pending-link.ts` encapsulates Redis get/set/del with fail-open error handling; `linkAccount` rate-limit key added (5/15min, IP-keyed); profile "Connected Accounts" card lists Email+Password row and each OAuth provider with icon, Unlink button (confirmation dialog), guarded against removing last auth method both client-side (button hidden) and server-side (`unlinkProviderAction`); orphaned `/api/auth/register` and `/api/auth/forgot-password` API routes removed (superseded by Server Actions) (Completed)
- **Items List View** - Dynamic `/items/[type]` route under `(app)` group; `getItemsByType` + `getItemTypeBySlug` added to `src/lib/db/items.ts` (slug resolved via DB OR query — no string manipulation); `ItemCard` component with type-colored left border, fixed `h-20`, circle icon, date; two-column responsive grid (`md:grid-cols-2`); empty state in `Card`; `ItemRow` on dashboard fixed to use `ring-1 ring-foreground/10` so `type-border-l` is no longer overridden by `border-color` shorthand; consistent `hover:bg-accent` + fixed `h-14` on `ItemRow`; `CollectionsGrid` hover unified; app logo wrapped in `<Link>` for client-side nav (Completed)
- **Items List Three-Column Layout** - Added `lg:grid-cols-3` to the items grid in `/items/[type]/page.tsx`; responsive layout: 1 col mobile, 2 col tablet (`md`), 3 col desktop (`lg`); pure CSS change, no server actions or utilities modified (Completed)
- **Refactor Pass** - Added `validatePassword` + `MAX_PASSWORD_LENGTH` to `src/lib/utils/validators.ts`; `withAuth` helper kept local in `src/actions/profile.ts`; dashboard section skeletons (`StatsSkeleton`, `CollectionsSkeleton`, `ItemsListSkeleton`) kept inline in `dashboard/page.tsx`; `useActionStateWithToast` hook extracted to `src/hooks/` (Completed)
- **Redis Cache-Aside** - Replaced `unstable_cache` with Upstash Redis cache-aside pattern; `src/lib/redis-cache.ts` is the single source of truth for all Redis keys (`CacheKeys`, `CacheTTL`, `RATE_LIMIT_NS`), TTLs co-located with keys; `withCache` + `invalidateCache` helpers; DB query functions in `items.ts`, `collections.ts`, `profile.ts` updated; `src/lib/cache.ts` provides `invalidateItemsCache`, `invalidateCollectionsCache`, `invalidateProfileCache`; `pending-link.ts` and `rate-limit.ts` import keys from `redis-cache.ts`; `profile.ts` actions call `invalidateProfileCache` after mutations; Router Cache disabled (`staleTimes` removed from `next.config.ts`); dead `next/cache` mock removed from test setup (Completed)
- **Items Page Fix** - Removed `export const revalidate = 60` leftover; refactored `/items/[type]/page.tsx` to fetch `getItemTypeBySlug` + `getCurrentUserId` in parallel then `getItemsByType` sequentially with resolved `itemType.name`; `ItemsGrid` converted from async server component to plain component receiving items as props; removed `ItemsGridSkeleton` (single `loading.tsx` skeleton instead of double skeleton) (Completed)
- **Utility and Architecture Refactoring** - Split `src/lib/utils.ts` into smaller focused utility files; extracted DB logic from server actions into `src/lib/db/profile.ts`; split `auth.ts` Server Actions into dedicated files under `src/actions/auth/`; moved `getCurrentUserId` to `src/lib/session.ts`; renamed `icon-utils.tsx` to `item-type-icon.tsx`; fixed `@auth/core/adapters` unlisted dependency in `src/auth.ts` (Completed)
- **Caching & Latency Optimization** - Replaced Upstash Redis caching with Next.js App Router Data Cache (`unstable_cache`) to provide single-digit millisecond latency via Vercel's edge network; decoupled ephemeral Redis usage (rate limiting, auth tokens) from permanent page caching; eliminated layout skeletons (`loading.tsx`) in favor of Next.js deferred transitions for instant UX; added global `nextjs-toploader` loading bar; fixed system item types pluralization bug (Completed)
- **Precise Loading Skeletons & Router Cache** - Removed `nextjs-toploader`; restored `loading.tsx` skeletons across Dashboard, Items, and Profile with precise DOM-matching shapes to eliminate layout shift during DB cold starts; enabled Next.js Client Router Cache via `staleTimes.dynamic = 30` in `next.config.ts` to provide 0ms instantaneous navigation between recently visited routes. (Completed)
- **Add Vercel Analytics & Cleanup** - Installed `@vercel/analytics`, added `Analytics` component to root layout, and cleaned up unused UI component exports (Completed)
- **Item Drawer** - `ItemDrawerContext` + `ItemDrawerProvider` client wrapper manages open/itemId state; `ItemDetailDrawer` (shadcn Sheet, right side) fetches full item via `GET /api/items/[id]` on click, shows `DrawerSkeleton` while loading; action bar with Favorite (yellow when active), Pin, Copy (functional), Edit, Delete (right-aligned); resizable via `useResizable` hook with left-edge drag strip; `ItemCard` and `ItemRow` wired with `onClick → openDrawer`; `ItemDrawerProvider` placed in `(app)` layout covering dashboard and items pages; `getItemById` added to `lib/db/items.ts`; `RouteContext` type added to `lib/api.ts`; seed made idempotent (find-or-create); Vitest tests added for `compareBySystemTypeOrder`, `getItemTypeBySlug` slug normalization, and `getSidebarItemTypes` null/live-userId paths (Completed)
- **Item Drawer - Edit Mode** - Implemented edit mode within the existing ItemDetailDrawer using conditional `DrawerEditContent`; built robust server action `updateItemAction` (Zod 4 compliant) and Prisma query `updateItem`; optimized cache invalidation via `updateTag`; resolved react architecture warning by replacing boolean state with `editingItemId`; updated cache invalidation architecture across profile actions for tighter cache consistency. (Completed)
- **Item Drawer - Delete Functionality** - Implemented the delete feature for items; added `deleteItem` to Prisma db layer using `deleteMany` for user safety; created `deleteItemAction` server action to invalidate items cache on success; wired `Trash2` button in `ItemDetailDrawer` to open a shadcn `Dialog` for confirmation; configured `toast.success`/`error` feedback, drawer auto-close, and router refresh; wrote full test suite for new action/db functions passing with 100% statement coverage. (Completed)
- **Item Create** - Implemented `ItemCreateDialog` with dynamic, conditional form fields driven by database item types; built `createItemAction` Server Action with DRY Zod validation (`baseItemSchema.extend`); added `createItem` Prisma query; centralized system type logic in `src/lib/utils/constants.ts`; resolved React hydration issues by properly scoping Client Components. (Completed)
- **Code Editor** - `CodeEditor` component using `@monaco-editor/react` with `vs-dark` theme, fluid height up to 400px, custom Monaco scrollbar; macOS-style window dots (red/yellow/green) in header with language badge and copy button; `ItemContentInput` and `ItemContentView` in `src/components/shared/item-content.tsx` route to `CodeEditor` for snippets/commands and `Textarea` for all other types; `useMonacoLanguage` hook resolves language strings to Monaco IDs; `useMonacoLanguageList` powers the `LanguageInput` datalist autocomplete; `ITEM_TYPES_WITH_CODE_EDITOR` constant drives all routing logic. (Completed)
- **Markdown Editor** - `MarkdownEditor` component with Write/Preview tabs, macOS-style header, copy button; `react-markdown` + `remark-gfm` for GFM rendering; `@tailwindcss/typography` `prose prose-invert` handles all element styling; auto-growing textarea (capped at 400px) with vertical scroll at cap; readonly mode shows Preview-only with "MARKDOWN" badge; `ITEM_TYPES_WITH_MARKDOWN_EDITOR = new Set(['prompt', 'note'])` drives routing; `ItemContentInput`/`ItemContentView` refactored into routing-only components with `CodeEditorInput`/`CodeEditorView` sub-components so Monaco only loads for code types; `contentEditorClassName`/`contentEditorWrapperClassName` props renamed from `codeEditor*` for accuracy. (Completed)
- **File & Image Upload** - Built `FileUpload` component with drag-and-drop and progress tracking. Added API routes `POST /api/upload` and `GET /api/download/[id]` to securely proxy traffic to Filebase (S3). Updated `ItemCreateDialog` to handle file uploads. Refactored Item Drawer architecture completely, introducing unified `react-hook-form` logic for edit mode and handling image previews & file downloads cleanly. Achieved 100% test coverage on all related server actions and AWS S3 utilities. (Completed)
- **Refactoring & Cleanup** - Extracted `baseItemSchema` to share between creation and edit forms; centralized database logic for tags (`buildTagsConnectOrCreate`) and relation mapping (`toItemDetail`); created reusable `DestructiveDialogFooter` and `DashboardListSkeleton`; removed unused imports and optimized SSR boundaries across UI components. (Completed)
- **Image Gallery View** - `ImageCard` component with `aspect-video`, `object-cover`, and `group-hover/card:scale-105` zoom on a 300ms transition; `ItemsGrid` routes image type to a dedicated 2/3-column thumbnail grid via `ITEM_TYPES_WITH_IMAGE_GRID` constant; title overlay with gradient on each card; non-image types continue to use `ItemCard` in the standard grid. (Completed)
- **File List View & Copy Buttons** - `FileRow` component with single-column Drive-style layout: file icon by extension (`FileTypeIcon` early-return component using `FILE_ICON_*_EXTS` constants), file name, file size, upload date, download button (programmatic anchor, no new tab), copy button (full download URL via `location.origin`); `ItemsGrid` routes `file` type to `FileRow` list via `ITEM_TYPES_WITH_FILE_LIST`; `Item` type extended with `content`, `url`, `fileName`, `fileSize` (promoted from `ItemDetail`); copy button added to `ItemCard` (absolute bottom-right corner, hover-reveal via `group/card`) and `ImageCard` (gradient overlay, hover-reveal); all card types use `group/card` + `opacity-0 group-hover/card:opacity-100` pattern; ghost button hover changed to `hover:bg-foreground/10` for visibility on any background; `cursor: pointer` global base rule added; `ReactNode` named import in `button.tsx`; `LucideIcon` from `lucide-react` in `file-row.tsx`; `<Image />` replaces `<img>` in `item-drawer-view-content.tsx` (Completed)
- **Codebase Audit Fixes** - `revalidatePath` scoped to `/dashboard` and `/items` (was layout-wide); email guard moved inside `withRateLimit` in `forgotPasswordAction`; `updateItem` adds `userId` to Prisma `update` where clause; `COLLECTION_INCLUDE` capped with `take: 50`; `baseItemSchema` renamed to `itemFormBaseSchema` in `validators.ts` + consumers updated; all `React.X` namespace refs replaced with named imports across 8 files; named interfaces added for `AuthPageBase`, `ExpiredTokenPage`, `MissingTokenPage`, `ErrorCard`, `ResendButton`; `location.origin` replaced with `getBaseUrl()` from new `src/lib/utils/url.ts` (uses `window.location.origin` in browser, `NEXTAUTH_URL` on server); `document.createElement('a')` justified with comment; `deleteOrphanedFile` now logs failures; `src/types/common.ts` adds `WithChildren` shared type; `getBaseUrl` re-export removed from `resend.ts`; tests for `getBaseUrl` caught a `??` vs `||` bug on empty env var (Completed)
- **Collection Create** - `CollectionCreateDialog` client component with name + description form using `react-hook-form` + `zodResolver`; `collectionFormSchema` added to `validators.ts` (shared between component and action); `createCollectionAction` server action in `src/actions/collections.ts` with auth guard, Zod validation, and cache invalidation; `createCollection` Prisma query in `src/lib/db/collections.ts` with `CreateCollectionInput` interface; `invalidateCollectionsCache` helper added to `src/lib/cache.ts`; "New Collection" button in the app top bar now opens the modal; success/error toasts + `router.refresh()` update all server-rendered collection data; 15 Vitest tests covering auth, validation, transforms, cache invalidation, and error paths (Completed)
- **Add Item to Collections** - `CollectionSelector` multi-select combobox (Popover + Command) with badge display and `data-checked` selection indicator; `collectionIds` field added to `itemFormBaseSchema`, `createItemAction`, `updateItemAction`, `CreateItemDialog`, and `ItemDrawerEditContent`; DB layer `createItem`/`updateItem` validate ownership of collection IDs before creating `ItemCollection` join rows; view mode shows collection badges via `DrawerSharedSections`; collections flow from `sidebarData` in layout to `ItemDrawerProvider` and `CreateItemDialog`; `ItemDetailDrawer` tracks `currentItem` locally so edits reflect immediately without reopening; `useCopyToClipboard` hook extracted — fires toast + flips icon to green `Check` for 2s, applied to all 6 copy buttons (drawer, ItemCard, ImageCard, FileRow, CodeEditor, MarkdownEditor); `ItemDetail` type alias removed, all callers use `Item` directly; `notFound()` moved before secondary fetches in items page; `CreateItemDialogAsync` wrapper inlined; `import * as React` replaced with named imports in 3 UI components; 4 new Vitest tests for collectionIds pass-through (66 total) (Completed)
- **Collection Selector UI Polish** - `CollectionSelector` trigger redesigned to show selected collection badges inline inside the button (replacing count text); separate badge block below the popover removed; `rounded-md border border-border p-3` wrapper removed from both drawer edit and create dialog contexts; `unselect` simplified to delegate to `toggleCollection`; duplicate `getSidebarItemTypes` + `getAllCollections` calls in `/items/[type]/page.tsx` replaced with single `fetchSidebarData(userId, null)` call; `fetchSidebarData` and `getItemsByType` parallelized in items page; `ItemDetailDrawer` state refactored — `savedItem` replaces `currentItem` so all item types render immediately from card data without a skeleton flash; `editing` guard tightened to `editingItemId !== null && editingItemId === displayItem?.id`; render prop indentation fixed in `CreateItemDialog` type selector (Completed)
- **Collections Pages** - Created `/collections` page listing user collections via `CollectionsGrid`, and `/collections/[id]` detail page displaying items dynamically based on their specific types. Linked collection card elements and sidebars, implemented matching loading skeleton wrappers, protected routes, and enabled router caching & revalidation. (Completed)
- **Optimizing Item Drawer Loading** - Created `PlainTextFallback` component matching the editor chrome (macOS dots header, monospace `<pre>`) as a Suspense boundary fallback; removed `loading: () => <Skeleton />` from `CodeEditor` and `MarkdownViewer` dynamic imports; wrapped both editors in `<Suspense fallback={<PlainTextFallback content={value} />}>`; `CodeEditorInput` and `CodeEditorView` sub-components handle Monaco language resolution with `PlainTextFallback` during the async `useMonacoLanguage` check; eliminates skeleton flash and layout shift when opening item drawers. (Completed)
- **Virtualized Item Lists** - Installed `@tanstack/react-virtual`; `useVirtualContainer` hook (src/hooks/) owns ResizeObserver + window-resize listener, scroll-margin calculation, optional column tracking, and a stable `getScrollElement` callback — all shared across virtualizers; `VirtualImageGrid` virtualizes the image gallery with aspect-ratio row heights; `VirtualItemGrid` virtualizes `ItemCard` grids (fixed 80px rows, 1/2/3 responsive columns at 768/1024px); `VirtualFileList` virtualizes `FileRow` lists (single column, ~64px estimated rows); `ItemsGrid` (items page) and `CollectionItemsGrid` (collections detail page) route to the appropriate virtual component per type; mixed-type collections fall back to a non-virtualized grid since row heights vary across `ImageCard`/`ItemCard`; fixed a `scrollMargin` positioning bug where `virtualRow.start` includes the margin so `top` must subtract it back. (Completed)
- **Collection Management** - Implemented edit, delete, and favorite functionality for collections. Added action buttons to `/collections/[id]` and a dropdown menu to `CollectionCard` instances across the dashboard and collections index. Developed modals for editing collection metadata and confirming deletion, ensuring deletion only unlinks items without deleting them. (Completed)
- **Optimizing Item Drawer Loading: Preload Editors** - Created `EditorPreloader` utilizing `requestIdleCallback` to fetch Next.js dynamic bundles (`CodeEditor`, `MarkdownEditor`, `MarkdownViewer`) and initialize Monaco Editor in the background. Extracted dynamic imports into a shared `dynamic-editors.tsx` to ensure component references match exactly across the preloader and UI, completely eliminating `next/dynamic` suspense boundaries on first open. Removed `PlainTextFallback` and refactored `useMonacoLanguage` to use the native `@monaco-editor/react` `useMonaco` hook for synchronous language resolution without `window.` hacks, ensuring the drawer opens with absolute zero latency. (Completed)
- **Refactor Scan Fixes** - `generateSecureToken()` extracted to `tokens.ts` and shared with `pending-link.ts`; `CopyButton` component created and applied across `ItemCard`, `ImageCard`, `FileRow`, `CodeEditor`, `MarkdownEditor`, and `ItemContentView` (6 call sites); both editors switched from raw `useState + setTimeout` copy state to `useCopyToClipboard`; item delete dialog updated to use `DestructiveDialogFooter` (matching collection delete); `PlainTextView` private component extracted in `item-content-view.tsx` to eliminate duplicate Mac-chrome-plus-pre blocks and add consistent `CopyButton` to all content view paths. (Completed)
- **Improve Backend Logging** - `createLogger(tag)` factory in `src/lib/logger.ts` returns scoped `{ info, warn, error }` loggers; format `[tag] message` in production, `HH:MM:SS [tag] message` in development; all raw `console.*` calls replaced across `cache.ts`, `api.ts`, `filebase.ts`, `resend.ts`, `actions/items.ts`, `actions/collections.ts`, and `api/download/[id]/route.ts`; cache invalidations now logged (items, collections, profile); item/collection mutations log success with title/id and userId; `SendEmailOptions.logTag` renamed to `operation`; 6 Vitest tests cover format, `error` arg branching, scope isolation, and dev-mode timestamp (Completed)
- **Infinite Scroll for Item Lists** - Replaced bulk data loading with cursor-based pagination to resolve a 2MB Next.js Data Cache limit crash on 500+ items; implemented `LightItem` type for minimal payload sizes; server-rendered first page with subsequent pages fetched async; utilized `@tanstack/react-virtual` for DOM virtualization combined with `IntersectionObserver` scroll triggers; unified store sync logic in `useInfiniteScrollSync` and centralized server actions via discriminated unions in `fetchMoreItemsAction`. (Completed)
- **Global Search / Command Palette** - `GlobalSearch` client component (cmdk `<Command shouldFilter={false}>`) in the app top bar; Cmd+K / Ctrl+K keyboard shortcut opens palette and closes the item drawer; click-outside handled via `containerRef`; hybrid search architecture — local filter against `ItemsStoreContext` loaded `LightItem`s and sidebar collections, plus debounced (300ms) `globalSearchAction` remote search; results deduplicated by ID with remote taking precedence; `CommandPrimitive.Loading` shown during in-flight requests; grouped results: Items (type icon, description preview) and Collections (color dot, item count); selecting an item navigates to its type page and opens the drawer, selecting a collection navigates to `/collections/[id]`; backend uses `fullTextSearchPostgres` preview feature + `pg_trgm` GIN indexes on `items(title, description, content)` and `collections(name, description)` via raw SQL migration; `LIGHT_ITEM_SELECT`, `toLightItem`, `COLLECTION_INCLUDE`, `mapCollection` exported from DB helpers for reuse; `closeDrawer` added to `ItemDrawerContext`; `ItemDrawerProvider` hoisted to wrap the full layout so the header can call it; `filebase.ts` `http` URL guard moved to the download route (correct layer); 4 Vitest tests cover auth, validation, success, and DB error paths. (Completed)
- **Settings Page** - Created a protected `/settings` route; added a Settings link in the sidebar user dropdown; moved Account Actions (Change Password, Delete Account) from the Profile page to the new Settings page; ensured proper type-safety and resolved UI anomalies. (Completed)
- **Editor Preferences Settings** - `editorPreferences` JSON column on `User` (Prisma migration); `EditorPreferences` type + `DEFAULT_EDITOR_PREFERENCES` + constants in `src/types/editor-preferences.ts`; `editorPreferencesSchema` Zod validator in `validators.ts`; `updateEditorPreferences` Prisma query in `lib/db/profile.ts`; `updateEditorPreferencesAction` server action with auth guard, validation, rate limiting, and cache invalidation; `EditorPreferencesProvider` React context auto-saves on field change with success toast; `EditorPreferencesForm` on `/settings` page (font size, tab size, word wrap, minimap, theme dropdowns/toggles); preferences applied to Monaco editor and Markdown editor; `useMonacoTheme` applies Monokai/GitHub Dark themes via `monaco-themes` loader; 20 Vitest tests for schema validation and server action paths. (Completed)
- **Favorites Page** - Star icon link in TopBar navigates to `/favorites` (protected route); `getFavoriteItemsPage` (cursor-paginated, `updatedAt desc`) and `getFavoriteCollections` added to DB layer with cache tags wired into `invalidateItemsCache`/`invalidateCollectionsCache`; `FavoriteItemsList` client component with infinite scroll (`useInfiniteScrollSync` + `IntersectionObserver`) and collapsible type-groups; `FavoriteItemRow` opens `ItemDrawer` on click; `FavoriteCollectionRow` navigates to `/collections/[id]`; empty state with dashed border; `loading.tsx` skeleton matches DOM shape; `getPaginatedItems` extended with optional `orderBy` param to eliminate duplication; `fetchMoreItemsAction` handles `type: 'favorites'` discriminant; 1 Vitest test added for the new action case. (Completed)
- **Favorite Toggle Buttons** - Added favorite toggle actions for Collections and Items; collection cards on the dashboard and collections page use interactive Star buttons; Item Drawer Star button added with instant UI cache updates; Favorites page handles un-favoriting seamlessly via client-store self-filtering; UI converted to server/client components where necessary to isolate interactivity. (Completed)
- **Pinned Items** - Pinned item functionality via `toggleItemPinnedAction` & `toggleItemPinned`, added `{ isPinned: 'desc' }` sorting to `getPaginatedItems`, and implemented static Pin and Star icon indicators across `ItemCard`, `ImageCard`, `FileRow`, and `ItemRow` components. (Completed)
- **Homepage Mockup** - Static marketing homepage prototype in `prototypes/homepage/` (index.html, styles.css, script.js); hero "chaos to order" section with rAF canvas physics (8 floating icons, bounce + mouse repel + rotation/scale pulse), pulsing CSS arrow, dashboard mockup; fixed nav with scroll-opacity; 6-card features grid with item-type accent colors; AI section with Pro badge, checklist, code editor mockup with AI tags demo; pricing with Free/Pro cards and monthly/yearly toggle ($8→$6/mo, $72/yr); CTA and footer; IntersectionObserver scroll fade-ins; fully responsive (hero stacks vertically, arrow rotates 90° on mobile). (Completed)
- **Homepage Next.js Conversion** - Converted the static marketing prototype into a responsive Next.js `(marketing)` route. Implemented `HomepageNav` with `IntersectionObserver`, `ChaosCanvas` floating icon physics, `PricingSection` toggle, and `FadeIn` scroll animations. (Completed)
- **Google OAuth** - Added Google OAuth provider to NextAuth alongside GitHub; handled credentials conflict with a redirect to `/link-account` flow; handled OAuth-only conflict by prompting users to sign in with their existing provider; updated the "Connected Accounts" card on `/profile` to show all linked providers, their icons, and a guarded Unlink button. (Completed)
- **Claude Code Config Refactor** - Moved coding standards and AI interaction rules to `.claude/rules/` per official docs; separated feature history into `context/history.md` to eliminate cold-start context bloat; fixed `refactor-scanner` agent tool names; cleaned up agent YAML description formatting. (Completed)
- **Codebase Quality Refactor** - Extracted `getItemCountByType` (cached) to `lib/db/items.ts`; replaced manual fetch-dispatch loop in `CollectionItemsGrid` with `useInfiniteItemsFetch`; collapsed `GitHubSubmitButton`/`GoogleSubmitButton` into `OAuthSubmitButton`; inlined `RemoveCredentialsDialog` and `UnlinkProviderDialog` into `connected-accounts.tsx` and deleted both files; relocated provider components from `src/components/providers/` to `src/providers/` and documented convention in coding standards; extracted `THEME_STORAGE_KEY` constant; created shared `WarningBanner` component; inlined `DashboardRecent` and `TopbarFavoritesLink` and deleted both files; fixed OAuth conflict detection to match on linked `Account.email` (not just `User.email`); fixed `handleOAuthConflict` to use `existingUser.email` as pending-link target; moved `APP_THEME_COLORS` and `EDITOR_THEME_COLORS` to `src/types/editor-preferences.ts`; added `login.test.ts` for `signInWithCredentials`. (Completed)
- **DB Query Optimisation (Load Side)** - Reduced transferred DB payload volumes using strict Prisma SELECTs, used raw `LEFT()` truncations for item previews, and implemented a generalized `React.cache()` deduping mechanism wrapper to eliminate duplicated same-request queries. (Completed)
- **Stripe Phase 1 (Core Infrastructure & Usage Limits)** - Created `src/lib/stripe.ts` (Stripe SDK initialization) and `src/lib/usage.ts` with testable limit constants and validation helpers (`canCreateItem`, `canCreateCollection`); extracted raw database queries to `src/lib/db/usage.ts` to adhere to PRISMA coding standards; successfully routed `isPro` status into session state natively via NextAuth `jwt`/`session` callbacks matching DB boolean flags; synchronized and tested robust limit gating on critical NextJS server actions (`createItemAction`, `createCollectionAction`) and API upload routes; 151 Vitest unit tests currently passing. (Completed)
- **Stripe Phase 2 (Webhooks & Checkout)** - Implemented Stripe Checkout and Billing Portal server actions, created webhook route to sync subscription states idempotently into the database, and built usage gating UI in Settings. Added client-side upgrade prompts and enforced Pro limits on the backend. Fixed DB query mock bugs. (Completed)
- **Bug Fix: Sign Out Flow & Cleanup** - Fixed an issue where the Next.js `NEXT_REDIRECT` error triggered a false-positive failure toast during sign-out. Refactored to use NextAuth's native client-side `signOut` from `next-auth/react` and deleted the redundant Server Action wrapper. Removed unused code flagged by the `cleanup` agent. (Completed)
- **Codebase Cleanup & Upgrade Page** - Extracted shared `PricingCards`/`BillingToggle` components to `src/components/shared/pricing-cards.tsx` and refactored `PricingSection` to use them; added dedicated `/upgrade` route with `UpgradePageClient` and Stripe checkout forms; added Upgrade link in app layout for free users; redirected upgrade prompt to `/upgrade`; inlined `validateItemId` helper and deleted `_utils.ts`; switched `createItem` to use Prisma `ContentType` enum; fixed `getItemDetails` cache tag; simplified `getPinnedItems` query; removed `isomorphic-dompurify` package; fixed Monaco Canceled error filter in `theme-provider.tsx` to check all console.error args. (Completed)
- **Item Drawer Layout Polish** - Adjusted item form fields to ensure consistent layout between view and edit modes; positioned content block at the top, and moved language combobox inline within the header next to the type badge. (Completed)
- **AI Auto-Tagging** - Added OpenAI-backed Pro-only tag suggestions via `src/actions/ai/generate-tags.ts`, `src/lib/ai/openai.ts`, and new tag input UI in `src/components/items/auto-tag-input.tsx` / `src/components/items/item-tags-field.tsx`, plus rate-limit/env/test updates. (Completed)
- **Stripe Billing & Pro Access** - End-to-end subscription billing with webhook idempotency, live Pro access verification (Redis cache + passive sync + orphan reconcile), checkout return finalization, recovery emails, and settings/upgrade portal flows for past_due/unpaid/paused; key areas `src/lib/billing/**`, `src/actions/billing.ts`, `/api/webhooks/stripe`, `/api/billing/checkout-return`, Prisma migration for sync/webhook state, and `scripts/validate-stripe-webhooks.ts`. (Completed)
- **AI Description Generation** - Pro-only AI description button on item and collection fields with preview-before-apply UX, shared AI field chrome (`ai-field-chrome`, `ai-description-field`, `ai-tags-field`), `AppUserProvider` for client tier gating, image thumbnail previews, and auth stale-cookie redirect fix; key files `src/actions/ai/generate-descriptions.ts`, `src/lib/ai/**`, `src/components/items/auto-description-input.tsx`, `src/providers/app-user-provider.tsx` (Completed)
- **Traffic Volume Optimization** - Reduced Vercel Fast Data Transfer via signed URLs (presigned direct upload, 302 redirect for copy-link), Service Worker image byte cache keyed by object path (survives URL rotation), slim Prisma selects across lists/search/collections/mutations, deferred drawer content, N+1 removal, thumbnail generation in browser, virtualized grids with lazy loading, and signed URL client cache with in-flight dedup; key files `download/[id]/route.ts`, `download/[id]/url/route.ts`, `upload/url/route.ts`, `public/sw.js`, `lib/db/items.ts`, `lib/db/collections.ts`, `lib/db/sidebar.ts`, `lib/storage/filebase.ts`, `hooks/use-pro-download-src.ts`, `scripts/backfill-image-thumbnails.ts` (Completed)
- **Datasource Loading Optimization** - Replaced 4 React Context providers and manual pagination stores with TanStack Query v5 (server state, infinite pagination, stale-while-revalidate caching) and Zustand stores (UI state: drawer, upgrade prompt, user flags, editor preferences); migrated all item list components to `useInfiniteItems` hook with intersection-observer auto-loading; replaced `react-virtuoso` with `@tanstack/react-virtual`; migrated Next.js cache from module-level Maps + AsyncLocalStorage to `'use cache'` directive + `cacheTag`/`cacheLife` across 25+ DB functions; key files `src/stores/`, `src/hooks/use-infinite-items.ts`, `src/providers/query-client-provider.tsx`, `src/lib/db/items.ts`, `src/lib/db/collections.ts`, `src/lib/infra/cache.ts` (Completed)
- **File/Image UX Hardening** - Migrated storage layer from Filebase to AWS S3 (`filebase.ts` → `s3.ts`) and unified upload/download APIs; eliminated redundant S3 404s on drawer open by removing hidden duplicate `<Image>` preload in `item-drawer-view-content.tsx` and guarding `ImageCard` error handler to defer only once; fixed download route to redirect to signed URL without `Content-Disposition: attachment` (no save dialog); added POST presigned policy support for size-enforced uploads; migrated item mutations from Server Actions to REST API routes (`/api/items/[id]` PATCH/DELETE) with TanStack Query cache updaters; added pinned items Zustand store + dashboard pinned section; key files `src/lib/storage/s3.ts`, `src/app/api/upload/`, `src/app/api/download/`, `src/hooks/use-infinite-items.ts`, `src/hooks/use-create-item.ts`, `src/hooks/use-update-item.ts`, `src/stores/pinned-items.ts` (Completed)
- **Upload Hardening** - Server-side `itemType`/`hasThumb` derivation from file extension, Redis-backed single-use upload tokens (`pending_uploads` keyed by S3 object key) with ownership assertion in `createItemAction`, presigned POST for thumbnails replacing PUT, background `sweepExpiredUploads` via `after()`, cross-type field stripping in `createItemSchema`; key files `src/lib/storage/upload-tokens.ts`, `src/app/api/upload/url/route.ts`, `src/actions/items.ts`, `src/components/shared/file-upload.tsx` (Completed)
- **Server Action → REST Migration** - Migrated all data mutations and queries from Next.js Server Actions to REST API routes under `src/app/api/`; introduced `useApiFormAction` hook as a `useActionState` equivalent for REST forms; added `patch()`/`del()` helpers to `api-fetch.ts`; extracted shared Zod schemas to `validators.ts`; all 50+ test files passing with direct route handler unit tests (`NextRequest` + JSON response); deleted `src/actions/` files for items, collections, profile, billing, AI generation, and auth (keeping OAuth-redirect actions); `updateEditorPreferencesAction` migrated to `PATCH /api/profile/editor-preferences`; `src/stores/editor-preferences.ts` switched to `patch()` removing the circular-import workaround (Completed)
- **App-Wide Touchscreen-Friendly Mobile UX** - Upsized all shadcn primitives to ≥44px tap targets under a `touch` custom variant (`pointer: coarse` or `width < 64rem`); responsive virtualized grid (1/2/3 list, 2/3 image columns) via `getListGridColumns`/`getImageGridColumns` helpers; hover-only affordances made always-visible on touch; item drawer full-width + swipe-to-close + full-edge slide animation; mobile sidebar swipe-to-close; app-wide 110% font bump on touch; favorites/profile/marketing header polished for mobile; editor chrome header shrunk; `useIsTouch` hook + Monaco gutter compacted on mobile; drawer body uses ScrollArea + `h-[70vh]` content block on all viewports; key files `globals.css`, `sheet.tsx`, `tanstack-virtual-grid.tsx`, `items-grid.tsx`, `use-swipe-to-dismiss.ts`, `use-virtual-container.ts`, `use-is-touch.ts`, `grid-columns.ts` (Completed)
- **Pino Logger Migration** - Replaced the custom `ScopedLogger`/`createLogger` wrapper with native Pino: added `pino` (dep) + `pino-pretty` (dev), created `src/lib/infra/pino.ts` (root `server-only` singleton, `LOG_LEVEL`-driven, `pino-pretty` transport in dev only) exporting `logger`; migrated ~40 server modules across `src/app/api/**`, `src/lib/**`, and `src/auth.ts` from `createLogger('tag')` to `logger.child({ tag })` with native call convention (bindings object first, message second) and `Error` context wrapped as `{ err: error }` to preserve stacks; typed injected loggers in `description-generation.ts`/`toggle-route.ts` as `Logger` from pino; inlined `toErrorMessage` into `api-fetch.ts`; replaced the logger import in `validate-billing-env.ts` with `console.warn` (keeps it importable by `next.config.ts`); updated 16 test files to mock `@/lib/infra/pino`; added `LOG_LEVEL` to `.env.example` + `src/types/env.d.ts`; deleted `src/lib/infra/logger.ts` and `logger.test.ts`; updated `coding-standards.md`/`nextjs-architecture.md` logging rules. (Completed)
- **Route Handlers + zod-openapi + openapi-fetch Migration** - Replaced the oRPC API layer with native Next.js Route Handlers across all 9 domains (38 endpoints) as explicit `src/app/api/<domain>/.../route.ts` files; bare Zod schemas in `src/lib/api/schemas/**` as single source of truth; generated OpenAPI 3.1 doc (`openapi.json`) via `zod-openapi` `createDocument()` from `src/lib/api/openapi/{paths,spec}.ts` + `scripts/generate-openapi.ts` (`npm run openapi:gen`); generated typed client `src/types/openapi.ts` consumed via `openapi-fetch`/`openapi-react-query` (`api`/`$api` in `src/lib/api/client.ts`); `authedRoute`/`authedRouteWithParams`/`publicRoute` helpers in `src/lib/api/route.ts` + `json`/`problem`/`parseOr422` in `src/lib/api/http.ts` preserving 401/403/404/422/429/500 behavior and IDOR-safe session `userId`; refactored `description-generation.ts` (`runProAiGeneration` throw→`AiGenerationResult`), `profile-helpers.ts` (throw→`FailureResult`), `toggle-cancellation.ts` (ORPCError→plain Error + `deliberate` flag), and DB date fields → ISO strings; replaced `use-orpc-form-action` with `use-api-form-action`; migrated 34 client files (components/hooks/store); deleted the `[...rest]` catch-all, `contract/**`, `router/**`, `orpc.ts`, `middleware.ts`, `openapi.ts`, `test/orpc.ts` and uninstalled 7 `@orpc/*` packages; rewrote `api-contract.md` + `nextjs-architecture.md`; 570 tests (54 files), lint 0, build exit 0, OpenAPI drift idempotent (Completed)
- **Password Recovery & OAuth Credential Bootstrapping** - Made the password-reset flow the single privacy-preserving way to set a password on any existing account, including OAuth-only ones. `applyPasswordReset` now sets a password when absent and marks `emailVerified` when null (also closing the unverified-credentials login gap); `triggerPasswordReset` resolves accounts via the new `findUserByAnyEmail` helper (User.email OR `accounts.some.email`) and always emails the primary; `registerUser` routes existing OAuth-only/secondary-email matches to a set-password email instead of a dead-end no-op or duplicate user. Hardened privacy/security: login validates password before revealing unverified state (Case 5) with a dummy `bcrypt.compare` for constant-time no-user/OAuth-only branches (Case 9), forgot-password returns immediately and defers lookup+send to `after()` (Case 9), `signIn` callback gates persisting `Account.email` on provider email-verification (Case 6, new `oauth-email.ts`), reset/verification tokens stored as SHA-256 hashes via `hashToken` (Case 8), and OWASP security-notification emails fire fire-and-forget to the primary on password set/change/reset, link/unlink, and password removal (Case 7, new `security-notification.ts`/`.html`). In-app `POST /profile/password` now also marks `emailVerified`. Key files `src/lib/auth/auth-service.ts`, `src/lib/db/users.ts`, `src/lib/auth/tokens.ts`, `src/lib/auth/oauth-email.ts`, `src/auth.ts`, `src/app/api/auth/{login,forgot-password}/route.ts`, `src/app/api/profile/{password,credentials,accounts/[id]}/route.ts`, `src/lib/emails/security-notification.*` (Completed)
- **UX/UI Improvements** - App-wide UX pass: grid-card/dashboard-row title truncation fix (own-row title + meta row); collection favorite-pill removal; collection detail header block; fully type-tailored item-create dialog widened to 860px with draggable `ResizableSplit` two-column editor layout (`resizable-split.tsx`), a no-vaul swipe-to-dismiss mobile `BottomSheet`, and a shared `ResponsiveFormDialog`/`FormDialogFooter` shell switched by `use-media-query`; `ItemFormFields` gains a `section` prop for per-layout placement; compact stats chip strip (`stat-chip.tsx`) with a custom radial `TotalItemsFanout` launcher; Favorites split into bookmarkable routes (`favorites/{layout,page,items,collections}` + `FavoritesTabNav`/`FavoritesEmpty`/skeletons); plus virtualized dashboard/grids with infinite scroll (`tanstack-virtual-grid.tsx`, `use-virtual-container.ts`), fullscreen editor chrome (`editor-chrome.tsx`), reworked drawer view + `image-lightbox.tsx`, shared touch gestures (`use-swipe-to-dismiss.ts`, `use-select-touch-swipe.ts`), and a marketing-component kebab-case rename; key files `src/components/items/item-card.tsx`, `src/components/dashboard/{item-row,stats-cards,stat-chip,total-items-fanout}.tsx`, `src/components/items/{item-create-dialog,item-form-fields,resizable-split}.tsx`, `src/components/ui/{bottom-sheet,responsive-form-dialog}.tsx`, `src/app/(app)/favorites/**` (Completed)
- **Separate, Verified Credential-Login Email** - Added `credentialEmail`/`credentialEmailVerified` to the User schema so Email & Password login uses its own independently-verified address; login resolution, forgot-password, and OAuth-conflict detection all match either column; `POST /api/profile/credential-email` + `POST /api/auth/confirm-login-email` handle add/change flows with Redis-hashed tokens; `/(auth)/confirm-login-email` page renders `TokenPasswordForm` (add) or `ConfirmEmailChangeForm` (change); `changeCredentialEmail` moves `User.email` + Stripe re-sync when addresses are in-sync; `bootstrapCredentialLogin` merges an OAuth account with a new credentials login on verify; OWASP security notifications sent to the primary; key files `prisma/schema.prisma`, `src/lib/db/users.ts`, `src/app/api/auth/confirm-login-email/route.ts`, `src/app/api/profile/credential-email/route.ts`, `src/lib/emails/credential-email.ts`, `src/components/profile/profile-email-section.tsx` (Completed)
- **Unique Database Logs** - Differentiated duplicate log messages in database methods (`profile.ts` and `collections.ts`) to make each statement unique and improve troubleshooting clarity (Completed)
- **Multiple OAuth Accounts** - Enabled users to link multiple OAuth accounts of the same provider to a single profile, deduplicated provider labels in the profile summary header, and added comprehensive unit tests; key files changed `src/components/profile/connected-accounts.tsx`, `src/lib/db/profile.ts`, `src/lib/db/profile.test.ts` (Completed)
- **Mobile Create Item Dialog Keyboard Adjustments** - Fixed keyboard overlay and auto-zoom on mobile create item dialog by adjusting bottom sheet max-height constraints and forcing a minimum 16px font size on touch devices; key files changed `src/components/ui/bottom-sheet.tsx`, `src/app/globals.css` (Completed)
- **Collection Edit Drawer Mobile** - Always mounted CollectionEditDialog and CollectionDeleteDialog inside CollectionDialogMount to preserve entry/exit animations on mobile viewports, using a reusable useLastNonNull hook and making collection nullable; key files changed `src/components/dashboard/collection-edit-dialog.tsx`, `src/components/dashboard/collection-delete-dialog.tsx`, `src/components/dashboard/collection-dialog-mount.tsx`, `src/hooks/use-last-non-null.ts` (Completed)
- **Primary Email Redirection & Warning UI** - Routed billing/recovery emails to the user's primary email address, added explicit logging for Stripe webhook customer updates, and improved the primary email change warning UI by moving the payment provider update notice to the warning banner; key files changed `src/lib/infra/resend.ts`, `src/lib/billing/webhook/stripe-webhook-event-handlers.ts`, `src/components/profile/main-email-selector.tsx` (Completed)
- **tweakcn presets + light/dark switching** - Sourced and generated 14 tweakcn theme presets; dropped `next-themes` and refactored layout to multiple root layouts for zero-flash SSR; persisted `appTheme` and `colorMode` in database preferences JSON; implemented browser-native circular reveal animations via View Transitions; dynamically mapped active theme preset colors to Monaco code and markdown editors; key files changed `src/app/(app)/layout.tsx`, `src/types/editor-preferences.ts`, `src/lib/editor/monaco-theme.ts`, `src/components/settings/editor-preferences-form.tsx`, `src/components/layout/sidebar/user-dropdown.tsx`, `src/components/shared/theme-initializer.tsx` (Completed)
- **On-demand Full-Size Image Preview** - Added S3 URL fetching for full-size image preview when opening the lightbox inside the item detail drawer. Implemented progressive image loading with low-res thumbnail base layer, high-res overlay on fade-in, and loading indicator pill. Added seamless fallback to preview thumbnail for non-Pro users or failed loads; key files changed `src/components/shared/image-lightbox.tsx`, `src/components/items/drawer/item-drawer-view-content.tsx` (Completed)
- **Theme Presets Expansion & UX Fixes** - Added 5 new tweakcn presets (vercel, mono, soft-pop, sage-garden, darkmatter); auto-regenerate OpenAPI after theme fetch/gen; fixed dark-light switch view transition DOM sync; fixed bottom sheet keyboard scroll to outermost container; card border changed from ring to border; key files changed `openapi.json`, `src/types/openapi.ts`, `package.json`, `scripts/fetch-theme.js`, `src/components/shared/dark-light-switch.tsx`, `src/components/ui/bottom-sheet.tsx`, `src/components/ui/card.tsx` (Completed)
- **Animate Item Type Dropdown** - Added smooth, fully opaque expand-down and collapse-up transitions to select dropdown popups with a 600ms duration, and resolved border/shadow clipping using negative clip-path insets; key files changed `src/app/globals.css`, `src/components/ui/select.tsx` (Completed)
- **Dashboard Tertiary Styling & Collapsible Sections** - Made Collections, Pinned, and Recent dashboard sections collapsible with Zustand persist middleware for localStorage-backed state; applied tertiary bg-muted card styling with CSS variable fallback; added useDirtyGuard + UnsavedChangesDialog for unsaved-changes protection in forms and item drawer; consolidated mobile create menu; replaced useDefaultEditorTheme boolean with three-way editorThemeMode preference; normalized UI tokens across shared components; key files changed `src/stores/dashboard-sections.ts`, `src/components/dashboard/dashboard-collapsible-card.tsx`, `src/components/dashboard/dashboard-content.tsx`, `src/hooks/use-dirty-guard.ts`, `src/components/shared/unsaved-changes-dialog.tsx`, `src/types/editor-preferences.ts`, `src/stores/editor-preferences.ts` (Completed)
- **Refactor: Dashboard Sections to DB Preferences** - Migrated collapsible dashboard section state from localStorage to DB-backed editorPreferences JSON column for cross-device sync; extracted DEFAULT_EDITOR_PREFERENCES and normalizeEditorPreferences to src/lib/utils/editor-preferences.ts; added DashboardSections type and dashboardSectionsSchema; cleaned up orphaned type files (auth.ts, search.ts, BillingRedirectData); key files changed `src/stores/dashboard-sections.ts`, `src/lib/utils/editor-preferences.ts`, `src/types/editor-preferences.ts`, `src/lib/utils/validators.ts`, `src/components/shared/editor-preferences-initializer.tsx` (Completed)
- **AI Field Badge Interactivity** - Made AI badges next to field labels clickable with the same cursor, tooltip, and trigger behavior as the AI action button; extracted `useAiTagsField` and `useAiDescriptionField` hooks so callers own the trigger state and can wire it to both the label badge and the field button; reduced AI button height by 1px (`h-7`); key files changed `src/components/shared/ai-field-chrome.tsx`, `src/components/shared/ai-tags-field.tsx`, `src/components/shared/ai-description-field.tsx`, `src/components/items/auto-tag-input.tsx`, `src/components/items/auto-description-input.tsx`, `src/components/items/item-form-fields.tsx`, `src/components/shared/collection-form-fields.tsx` (Completed)
- **No-Flash Collapse State & App Shell Polish** - Dashboard section collapse state persisted via `ds-layout` cookie with blocking `ThemeScript` inline script setting `data-section-*` HTML attributes before first paint; `writeLayoutCookie` utility syncs cookie and attributes on every toggle; skeleton cards carry matching `data-section` attributes; app shell gets dot-grid background, glass topbar, and ambient glow blobs; auth dynamic forms gain loading skeletons; key files `src/lib/utils/layout-cookie.ts`, `src/components/shared/theme-script.tsx`, `src/components/dashboard/dynamic-page-skeleton.tsx`, `src/stores/dashboard-sections.ts`, `src/app/(app)/layout.tsx`, `src/app/globals.css` (Completed)
- **OAuth Conflict Link Flow UX** - Replaced the dead-end "Account already exists" message on `/link-account` with actionable OAuth sign-in buttons; user attempting GitHub login with a Google-only account now sees "Sign in with Google" which redirects back to the same `/link-account?token=...` URL, landing them in the auto-link confirm screen; `signInWithOAuthForLinkAction` added to `src/actions/auth/login.ts`; key files `src/app/(auth)/link-account/page.tsx`, `src/actions/auth/login.ts` (Completed)
- **Auth Hardening & Mobile Editor UX** - 30-min idle session timeout (`session-idle.ts`, `applySessionActivity`), IP rate limit before bcrypt in login route, password max-length guard; atomic `getdel` for pending-link tokens, consume deferred until after `linkPendingAccount` succeeds, P2002 race safety, `LINK_INTENT_COOKIE` cleared after auto-link, email stored on JWT to eliminate extra DB call in `session()`; touch fullscreen expand + drag-to-collapse on mobile editor chrome, `useEditorHeaderDrag` hook, ESC key capture for fullscreen; controlled create-dialog type sync, responsive editor preferences layout; `redisOp<T>` helper extracted in `pending-link.ts`; key files `src/lib/auth/session-idle.ts`, `src/lib/auth/pending-link.ts`, `src/lib/auth/auth-service.ts`, `src/actions/auth/link.ts`, `src/auth.ts`, `src/lib/db/users.ts`, `src/components/ui/editor-chrome.tsx`, `src/components/items/item-create-dialog.tsx`, `src/components/settings/editor-preferences-form.tsx` (Completed)
- **Editor Keyboard-Survival Chrome** - Reworked `EditorChromeShell` to render editor content exactly once inside a single never-remounted `document.body` portal that animates between an in-flow sentinel rect (collapsed) and the visual viewport (fullscreen), so focus and the on-screen keyboard survive the expand/collapse morph on touch; tap-anywhere-to-expand via a `display:contents` wrapper; readonly Monaco sets `inputMode="none"` to suppress the keyboard while staying tap-expandable; markdown "show keyboard" button re-focuses the textarea within the pointer gesture; create-dialog dirty guard no longer flags a bare item-type switch; key files changed `src/components/ui/editor-chrome.tsx`, `src/components/ui/code-editor.tsx`, `src/components/ui/markdown-editor.tsx`, `src/components/items/item-create-dialog.tsx` (Completed)
- **No-Flash Collapse Persistence (DB Source of Truth) & UI Polish** - Dashboard section AND sidebar collapse state now render correctly from first paint with no expand→collapse flash: server reads the `ds-layout` cookie to seed each card's `defaultOpen`/sidebar `initialCollapsed` so SSR matches the first client render, stores seed from the same cookie, and a `useSyncExternalStore` hydration probe adopts the store post-mount; the DB (`editorPreferences.sidebarCollapsed` + `dashboardSections`) is the single source of truth via `PATCH /profile/editor-preferences`, with both toggles persisting through `useEditorPreferencesStore.updatePreferences` so neither can clobber the other's value with a stale field. Bundled UI polish: dialog grow-from-origin morph, editor-chrome height-based expand/collapse morph, item-drawer mobile grab pill. Also added a Stripe webhook OpenAPI documentation surface (top-level `webhooks` section driven from `STRIPE_WEBHOOK_EVENT_CONFIG`, `customer.updated` event) — docs only, not runtime validation. Key files `src/lib/utils/layout-cookie.ts`, `src/stores/dashboard-sections.ts`, `src/stores/editor-preferences.ts`, `src/components/dashboard/dashboard-collapsible-card.tsx`, `src/components/layout/sidebar/collapsible-sidebar-shell.tsx`, `src/components/layout/sidebar/sidebar-skeleton.tsx`, `src/lib/utils/editor-preferences.ts`, `src/lib/utils/validators.ts`, `src/lib/api/openapi/webhooks.ts`, `src/lib/api/schemas/webhooks.ts`, `src/lib/billing/config/stripe-webhook-config.ts` (Completed)
- **AI Explain Code** - Pro-only "Explain" affordance for snippet/command items: new `POST /ai/explain` route reads canonical content server-side scoped to the session userId (IDOR-safe), truncates to `EXPLAIN_MAX_INPUT_CHARS` (8000), and returns `{ explanation }` via `runProAiGeneration` + `runOpenAiCompletion` with `AI_MODELS.DEFAULT`; explanation persists to `item.description` (max raised 500→2000) through an explicit Save that reuses `useUpdateItem` for the PATCH + cache sync, with a close-time `UnsavedExplanationDialog` + `ReplaceDescriptionDialog` wired through `useDirtyGuard`/`sheetCloseRef`; drawer code-editor header gains Code/Explain tabs, a styled Tooltip on the Explain/Save buttons, and a Crown gate for free users; new `aiExplain` rate-limit key; key files `src/app/api/ai/explain/route.ts`, `src/lib/ai/explain-response.ts`, `src/hooks/use-explain-code.ts`, `src/components/shared/item-content-view.tsx`, `src/components/shared/unsaved-explanation-dialog.tsx`, `src/components/shared/replace-description-dialog.tsx`, `src/lib/db/items.ts`, `src/lib/api/schemas/ai.ts`, `src/lib/utils/validators.ts` (Completed)
- **AI Prompt Optimization** - Pro-only "Optimize" affordance for `prompt` items (gated strictly to `prompt`, not `note`): new `POST /ai/optimize` route reads canonical prompt content server-side scoped to the session userId (IDOR-safe), truncates to `OPTIMIZE_MAX_INPUT_CHARS`, and returns `{ prompt }` via `runProAiGeneration` + `runOpenAiCompletion`; `parseAiOptimizedPromptResponse` strips a markdown fence, accepts raw text or `{ prompt }` JSON, and clamps to `OPTIMIZE_MAX_OUTPUT_CHARS`; result persists to `item.content` through `useUpdateItem` (optimistic cache sync + rollback) behind a replace-confirm flow; `MarkdownContentView` gains a `PromptOptimizeHeader` (Crown gate for free users, Sparkles "Optimize", then Original/Optimized tabs + "Apply"), drawer close guarded via `useDirtyGuard` while an unapplied optimized prompt exists; new `aiOptimize` rate-limit key; shared `confirm-dialog.tsx` replaces the per-purpose replace/unsaved dialogs; bundled refactor moved `signed-download-cache` → `src/lib/api/` and `theme-transition` → `src/lib/dom/` (+ new `src/lib/dom/create-item-trigger.ts`, `src/lib/ai/markdown.ts`); key files `src/app/api/ai/optimize/route.ts`, `src/lib/ai/optimize-response.ts`, `src/hooks/use-optimize-prompt.ts`, `src/components/shared/item-content-view.tsx`, `src/components/shared/confirm-dialog.tsx`, `src/components/items/drawer/item-drawer-view-content.tsx`, `src/lib/api/schemas/ai.ts`, `src/lib/api/openapi/paths.ts`, `src/lib/infra/rate-limit.ts`, `src/lib/utils/constants.ts` (Completed)
- **UI Skins — Dashboard (Phase 1)** - Per-user `uiSkin` preference adding 9 selectable dashboard skins (classic default + aurora/editorial free; spatial/command-deck/orbital/mission-control/neon-grid/holographic Pro), persisted in the existing `editorPreferences` JSON column with no Prisma migration; resolved server-side in `dashboard/page.tsx` with a server-authoritative Pro gate (`resolveAccessibleSkin` downgrades a stored Pro skin to `classic` for free users) and streamed in a Suspense boundary for no-flash first paint; `DashboardSkinShell` dispatches to one of nine skin components under `src/components/dashboard/skins/`, sharing reusable pieces (`resolveSkinData` awaited-data helper, `TypeDistributionBars`/`TypeDistributionSegments`, `SkinCollapsibleSection`, `DashboardCollectionsList`/`DashboardPinnedItems`/`DashboardRecentItems`); two new cached, userId-scoped DB aggregations `getItemTypeDistribution` (per-type `groupBy`) and `getDashboardActivity` (raw-SQL per-day 12-week series for the contribution heatmap, window anchored to one clock to avoid a UTC-midnight skew), both gated in `page.tsx` so only the consuming skins fetch them; `mission-control` lazy-loads Recharts (donut/sparkline) + react-activity-calendar (heatmap) via `next/dynamic` `ssr:false` inside a `'use client'` island so they stay off the default/classic bundle; vendored Magic UI background/border FX (`DotPattern`, `RetroGrid`, `OrbitingCircles`, `AnimatedGridPattern`, `BorderBeam`); settings picker (`editor-preferences-form.tsx`) renders locked Pro skins that route to the upgrade prompt instead of selecting; removed the old dashboard section collapse-persistence (`dashboardSections` pref + Zod schema, `ds-layout` section cookie, `dashboard-sections` store, `normalizeDashboardSections`/`DashboardSections`, `initialSections`/`defaultOpen` plumbing — sidebar collapse retained); new deps `recharts` + `react-activity-calendar`; OpenAPI regenerated; key files `src/types/ui-skins.ts`, `src/types/editor-preferences.ts`, `src/lib/utils/editor-preferences.ts`, `src/lib/db/items.ts`, `src/app/(app)/dashboard/page.tsx`, `src/components/dashboard/dashboard-content.tsx`, `src/components/dashboard/skins/**`, `src/components/settings/editor-preferences-form.tsx`. Phases 2 (ambient `data-skin` shell) and 3 (auth/marketing polish) deferred (Completed)
- **Fix: Mobile Document Scroll + Window-Virtualized Grid** - Mobile app shell now lets the *document* scroll so the browser URL bar collapses, while desktop keeps the fixed `<main>` scroller: shell is `min-h-dvh` + `lg:h-screen lg:overflow-hidden`, topbar `sticky top-0 lg:static`, `<main>` `overflow-x-hidden lg:overflow-y-auto`. `TanStackVirtualGrid` branches on `useIsTouch()` up front into `WindowVirtualGrid` (`useWindowVirtualizer`, mobile) vs `MainVirtualGrid` (`useVirtualizer` bound to `<main>`, desktop) — stable per device so it never remounts in normal use — sharing a `useGridRows` row-matrix hook and a presentational `VirtualGridBody`; `useVirtualContainer` gains a `windowMode` flag selecting absolute-document vs `<main>`-relative `scrollMargin`, with the coordinate-frame math extracted to `src/lib/utils/scroll-margin.ts` (`computeWindowScrollMargin`/`computeMainScrollMargin`) and unit-tested. Bundled touch-editor fixes: the collapsed `EditorChromeShell` `position:fixed` portal applies a `clip-path` of its overflow-clipping ancestors' combined bounds so it stops painting over the form header/footer once the document scrolls, and swipe-to-dismiss ignores gestures starting inside the `data-editor-overlay` portal so dragging a maximized editor never closes the surrounding sheet. Verified via lint + 840 unit tests + production build; URL-bar collapse itself still pending real-device confirmation. Key files `src/app/(app)/layout.tsx`, `src/components/items/tanstack-virtual-grid.tsx`, `src/hooks/use-virtual-container.ts`, `src/lib/utils/scroll-margin.ts`, `src/components/ui/editor-chrome.tsx`, `src/hooks/use-swipe-to-dismiss.ts` (Completed)
- **AI Usage Meter** - Proactive, read-only Pro-only surface of how many AI calls remain per feature (`aiTags`/`aiDescription`/`aiExplain`/`aiOptimize`, each a 20/hr rolling Upstash sliding-window) and when each renews, instead of only discovering the cap via a 429 — no DB migration, no change to enforcement. `getAiUsage(userId)` in `rate-limit.ts` fires all four non-consuming `limiter.getRemaining()` reads under one `Promise.all` (collapsed to a single `/pipeline` round-trip via `enableAutoPipelining: true` in `redis.ts`) and always fails open to full budget on any error/null limiter, logging once via `logger.child({ tag: 'ai-usage' })`; new `GET /ai/usage` (`authedRoute`, no `rateLimit` so it never spends a token, own `isPro` 403) returns `aiUsageOutput` `{ features }`. Client `useAiUsage()` (`$api.useQuery` with `enabled: isPro`, `refetchIntervalInBackground: false`, and a function `refetchInterval` that stops polling when every feature is at full budget) + `useInvalidateAiUsage()` + a `runAiMutation`/`useAiMutation` wrapper that invalidates `/ai/usage` in `finally`; the five AI consumers (optimize/explain/tags/description/collection-description) route through it, enforced by a `no-restricted-syntax` ESLint rule banning direct `api.POST('/ai/…')` outside the hook. Per-skin full-width `AiUsageWidget` renders a `@container` 4-up sub-grid of mini feature-cards (`NumberTicker` `remaining/limit` + slim linear bar + `formatRenewIn` "next slot in Xm") with a shape-matched `Skeleton`; Pro users lose the constant "∞/Unlimited" stat tile (4-up→3-up reflow), `holographic`/`editorial` drop the Favorites tile for all users. Magic UI `number-ticker` vendored (eslint-disabled). Key files `src/lib/infra/rate-limit.ts`, `src/lib/infra/redis.ts`, `src/app/api/ai/usage/route.ts`, `src/lib/api/schemas/ai.ts`, `src/lib/api/openapi/paths.ts`, `src/hooks/use-ai-usage.ts`, `src/lib/utils/format.ts`, `src/components/dashboard/ai-usage-widget.tsx`, `src/components/dashboard/skins/**`, `src/components/ui/number-ticker.tsx`, `eslint.config.mjs`, `.agents/rules/api-contract.md` (Completed)
- **Fix: Idle Session Timeout Enforcement** - The 30-min idle-timeout invalidation was only wired into the node `auth.ts` jwt callback, so the proxy/route gate (`auth.config.ts`, run in `proxy.ts`) never invalidated an idle session before `authorized` evaluated it. Added a `jwt({ token, user })` callback to `authConfig.callbacks` (declared before `authorized`) that calls `applySessionActivity(token, Boolean(user))`, returns `null` on idle (emptying the session so protected routes redirect to `/sign-in`) and otherwise refreshes `token.lastActiveAt`; kept edge-safe (pure `Date` math, no DB/bcrypt, no server-only Pino import) since `auth.ts` still owns the durable DB-backed refresh + logging. Named `JwtParams` interface per coding-standards. `session-idle.test.ts` now exercises the REAL `authConfig.callbacks.jwt` (via fake timers) rather than a re-implemented copy, so a regression in the actual wiring is caught. Key files `src/auth.config.ts`, `src/lib/auth/session-idle.test.ts` (Completed)
- **Fix: Dashboard Skin Skeletons Match Post-AI-Meter Layouts** - Follow-up to the AI Usage Meter: every per-skin Suspense skeleton in `src/components/dashboard/skins/skeletons.tsx` is reworked to be pixel-identical to its now-changed loaded skin (the meter feature dropped the dead "∞ / slots-left" stat tile for Pro reflowing stat rows 4-up→3/2-up, dropped Favorites tiles, reordered sections, and added a full-width AI Usage section at each skin's foot — none of which the skeletons reflected). `DashboardSkinFallback` gains an `isPro` prop (layout diverges by tier; the six Pro-only skins always render their Pro layout, only classic/aurora/editorial branch); new `AiUsageSkel` mirrors `AiUsageWidget` (per-skin treatment classes matching `SKIN_TREATMENTS`, `@container` 2-up/`@md:4-up` meter grid) with a Card-based `ClassicAiUsageSkel` variant; per-skin stat-area rewrites (aurora ring + separate distribution card, editorial 3→2 figures, single-Collections stat for spatial/orbital/command-deck/neon-grid, mission-control 2-up KPI w/ sparkline, holographic hero + single card) and section-order fixes. `page.tsx` passes the real `isPro`; `loading.tsx` derives it via `isProSkin(skin)` (no Pro cookie — Pro-only skins only load for Pro users, free skins assume non-Pro). Verified via lint + Playwright capture of classic & mission-control skeletons; no component tests (repo rule). Key files `src/components/dashboard/skins/skeletons.tsx`, `src/app/(app)/dashboard/page.tsx`, `src/app/(app)/dashboard/loading.tsx` (Completed)
- **Dashboard Skeleton Mobile & Margin Alignment** - Aligned loading skeletons' shapes, positions, and margins across all 9 skins on both desktop and mobile viewports, including adding `relative overflow-hidden` to holographic skeleton cards to prevent mobile layout overflow and replacing `space-y` header spacing with `mt-1` margins on children to eliminate visual vertical jumps. Key files: `skeletons.tsx` (Completed)
- **Brain Dump (AI File-to-Items Splitter)** - Pro-only AI-powered file/paste splitter with background streaming/resume support, a Kanban review board (`/parse`), batch collection target, and source deep-linking; key files changed `src/app/api/ai/brain-dump/**`, `src/components/parse/**`, `src/lib/ai/brain-dump.ts`, `src/lib/db/ai-parse-jobs.ts` (Completed)
- **Brain Dump v1.5 (Re-parse + Source Labels)** - Added `POST /api/ai/brain-dump/{jobId}/re-parse`: IDOR-scopes the original job, re-reads its durable `sourceItemId`, validates Pro access and source readability *before* consuming the `aiBrainDump` hourly token, then creates a fresh parse job (new parse window, source label, truncation state, seeded collection name); a missing/deleted/ineligible/unreadable/too-short source spends no token, and job-creation failure refunds it. The route is contract-first (Zod response schemas in `schemas/ai.ts` + `paths.ts` declaration, generated OpenAPI types) and the review page exposes a re-parse action that states it spends a new hourly token, reports failures, and navigates to the new job on success. Source labels now distinguish concurrent jobs: `/parse` list rows show `sourceName`, the entry-card awaiting-review badge tooltip lists pending source names, and the review header identifies the current source with its stash-item link. Discard still best-effort cancels the OpenAI background response idempotently without blocking. Tests cover re-parse auth, Pro/rate-limit gates, IDOR and unavailable-source failures, fresh-job creation, and token refund. Key files `src/app/api/ai/brain-dump/[jobId]/re-parse/**`, `src/lib/db/ai-parse-jobs.ts`, `src/lib/api/schemas/ai.ts`, `src/lib/api/openapi/paths.ts`, `src/hooks/use-brain-dump.ts`, `src/hooks/use-ai-usage.ts`, `src/components/parse/**`, `src/lib/utils/derive-source-label.ts` (Completed)
- **Brain Dump v2 + v2.5 + v3** - Added stash integration, notes tab, advisory de-dup badges, bulk board actions, lazy TTL cleanup, job lifecycle states (closed/failed history, terminal mapping), collection confirmation, language sets, and live item type changes; key files changed under `src/app/api/ai/brain-dump/`, `src/components/parse/`, `src/lib/ai/`, `src/lib/db/ai-parse-jobs.ts` (Completed)
- **Next.js Client-Server Boundary Fix for brainDumpSkinPanel** - Resolved import boundary error by relocating skin chrome styling constants and the `brainDumpSkinPanel` helper function from the client component `brain-dump-widget.tsx` to a new shared utility file `brain-dump-skin-panel.ts`, ensuring server-rendered skeletons (`skeletons.tsx`) can safely import and execute it (Completed)
- **Brain Dump Paste to Snippet** - Pasted plain text in the Brain Dump page is now saved as a Snippet item type with language set to 'Plain Text' instead of a Note. Adjusted S3/note helper getSourceText to support snippet items as source candidates, and updated listParseSourceCandidates to search for both note and snippet items with the brain-dump tag. (Completed)
- **Hook Consolidation & Single-Use Hook Collapse** - Consolidated and grouped related custom hooks and collapsed single-use hooks back into their caller files to reduce folder bloat in `src/hooks`. Combined `useRestrictedAction` and `useRestrictedDownload` into a single `use-restricted.ts` hook. Inlined search shortcuts `useGlobalSearchShortcuts` into `global-search.tsx`, sentinel auto-fetch `useAutoFetchNextPage` into `favorite-items-list.tsx`, touch swipe `useSelectTouchSwipe` into `item-create-dialog.tsx`, Action state toast wrapper `useActionStateWithToast` into `link-account-form.tsx`, and AI rewrite configurations (`useExplainCode` and `useOptimizePrompt`) directly inside `item-drawer-view-content.tsx` and `item-content-view.tsx`. Deleted 8 obsolete custom hook files. Verified with `npm run lint` and `npm run test:run` (1148 tests passing). (Completed)
- **Collections & Profile Client-Cache (TanStack Query)** - Migrated all server-derived client state from Zustand stores to TanStack Query: `app-user-flags` and `editor-preferences` stores deleted; `/profile/me` + `/billing/context` + `/collections/{id}` routes added; `useUserProfile`, `useCollections`, `useCollection`, `useBillingContext` hooks with SSR `initialData` seeding; `useInvalidate` switch extended to `collections`/`userProfile`/`billingContext`; `CollectionsGrid`, `FavoriteCollectionsList`, `CollectionDetailHeader`, sidebars, `BillingSettings`, `BillingActions`, and profile dialogs migrated to query-cache reads/invalidations (no `router.refresh()` except skin change); all dynamic route params typed with `parseOr422` Zod pattern; `query-keys.test.ts` and `user-billing-state.test.ts` added. Also bundled: Brain Dump review board top-widget layout rework — responsive 4-col/2-row CSS grid with `minmax(min-content,1fr)` status column, `display:contents` action-pair wrapper, and staggered container-query button-label hiding that progressively collapses to icon-only as the widget narrows (Completed)

- **Brain Dump Drawer Trash Actions & Mobile Overlay Fixes** - Trash drafts in the edit drawer now expose Restore + Delete-forever (matching the inline card overlay) instead of a plain Delete: new `EditDraftDrawer` props `inTrash`/`onRestore`/`onDeleteForever` wired through `parse-draft-card.tsx`, reusing the board's optimistic `persistMove` handlers so the actions close the drawer synchronously; added a `DrawerAction` worded action-bar button (icon + collapsing label + tooltip) as the verbose sibling of `IconAction`. Permanent-delete confirm `DialogContent` gains `elevated` so it lifts to z-[60] above the edit-drawer Sheet. Mobile overlay fixes: `overscroll-contain` on the drawer ScrollArea viewport (`drawer-shared.tsx`) to stop iOS URL-bar flash on overscroll, Monaco editors scroll inside sheets, side sheets fill the dynamic viewport (`sheet.tsx`, `globals.css`); added `isStreamingPhase` predicate in `use-brain-dump.ts` as the single source of truth for the streaming glow/progress UI; new `glow-border.tsx` UI component plus `parse-progress.tsx`/`parse-review-board.tsx` glow wiring (Completed)
- **Mobile Item Full-Screen View & lib/dom Migration** - On touch devices opening an item now renders a full-screen, document-flow view (no Sheet) so the mobile browser retracts its URL bar on scroll; desktop keeps the right-side Sheet. `useIsTouch` selects which shell `ItemDrawerProvider` mounts: new `MobileItemPaneSlider` (page kept mounted via React 19.2 `<Activity>`, three slider phases idle/sliding/settled, swipe-right-to-dismiss through the dirty-edit guard, grip pill inside the panel) vs the desktop `ItemDetailDrawer`. Shared drawer body via a `fullScreen` flag across `drawer-shared`, `item-drawer-view-content`, `item-drawer-edit-content`; `SheetTitle` swapped for a visually-hidden `<h1>` in full-screen. `useVirtualContainer` gained a `frozen` flag to hold document-time cols/width/scrollMargin (backdrop width freeze) and window scroll captured in `useItemDrawerStore.openScrollY`/re-asserted on close. On-screen-keyboard inset read from `useVisualViewport` scrolls the focused input above the keyboard in both modes. Bundled lib migration: `layout-cookie` moved `src/lib/utils → src/lib/dom`, new `src/lib/dom/drawer-selectors.ts`, `item-drawer` store renamed to `item-drawer-store` with tests. Verified `npm run lint` + `npm run test:run` (1217 tests passing) (Completed)
- **Hook & Lib Domain-Folder Reorg** - Regrouped 38 custom hooks from flat `src/hooks/` into six domain folders (`ai/`, `items/`, `billing/`, `profile/`, `editor/`, `ui/`); relocated `src/lib/app/{profile-helpers,sidebar-data}` → `src/lib/services/`, and merged `src/lib/editor/monaco-theme.ts` + `src/lib/utils/toast-error.ts` into `src/lib/dom/`. Updated all ~110 import sites plus `nextjs-architecture.md` (folder tables, hook-organization note, `storage`→`storage-client` path fix) and the `cleanup` skill docs. Hardened seven server modules with a missing `import 'server-only'` first line (`lib/ai/openai`, `lib/infra/redis`, `lib/session`, four `lib/billing/emails/*`). Pure structural refactor — no behavior change. Verified `npm run lint` + `npm run test:run` (1209 tests passing) (Completed)
- **Shared Swipe-Close Hook & Brain Dump Mobile Full-Screen Edit** - Extracted `useMotionSwipeClose` hook (`src/hooks/ui/use-motion-swipe-close.ts`) shared by `ItemFullScreenView` and the new `MobileDraftFullScreenView`; refactored `MobileItemPaneSlider` to a `renderPane` callback API so item-latch lives in `ItemDrawerProvider`; `MobileDraftFullScreenView` in `parse-draft-card.tsx` gives the brain dump edit drawer a swipe-right-to-close full-screen shell on touch; `draftToFullItem` extracted to `src/lib/utils/brain-dump-draft.ts` with Vitest tests; key files `src/hooks/ui/use-motion-swipe-close.ts`, `src/components/items/drawer/item-detail-drawer.tsx`, `src/components/items/drawer/mobile-item-pane-slider.tsx`, `src/components/parse/parse-draft-card.tsx`, `src/providers/item-drawer-provider.tsx`, `src/lib/utils/brain-dump-draft.ts` (Completed)
- **Hook Refactoring Opportunities** - Refactored custom hooks: standardized `useMediaQuery` to use `useSyncExternalStore` for concurrent-rendering safety, internalized the stale-closure ref-syncing (`isOpen`) inside `useMotionSwipeClose` to eliminate caller boilerplate, and collapsed/simplified the entity-specific cache invalidation hooks (`useInvalidateCollections`, `useInvalidateUserProfile`, `useInvalidateBillingContext`, `useInvalidateAiUsage`) to use the generic `useInvalidate` hook directly at call sites. Verified via `npm run lint` and `npm run test:run` (1222 tests passing). (Completed)
- **Draft Drawer Global Store Provider** - Moved the mobile brain-dump draft-edit drawer out of the draft card's React subtree into a global `DraftDrawerStore` (Zustand, single slot — item + six callbacks, ownership-guarded `closeDrawer`) + `DraftDrawerProvider` (store-connected mount above `<main>`, latches the last item for the close slide), mirroring `ItemDrawerProvider`; this lets the mobile slider render outside `<main>`'s `overflow-x-clip` containing block. `EditDraftDrawer` in `parse-draft-card.tsx` now writes open state + callbacks to the store via effect (re-syncs on identity/prop changes) instead of rendering `MobileItemPaneSlider` inline; desktop `DrawerShell` path unchanged. Added `stickyHeader` prop through `DrawerLayout`/`DrawerContainer`/`FullScreenDrawerContainer`/`ItemDrawerEditContent` for the fixed-inset overlay case (sticky header instead of fixed, no spacer measurement), and a `noPage` mode to `MobileItemPaneSlider` (stays `fixed inset-0 z-40` when there is no page backdrop). Key files `src/stores/draft-drawer-store.ts`, `src/providers/draft-drawer-provider.tsx`, `src/app/(app)/layout.tsx`, `src/components/parse/parse-draft-card.tsx`, `src/components/items/drawer/{drawer-shared,item-drawer-edit-content,mobile-item-pane-slider}.tsx`; 4 Vitest tests for the store (seed/replace/clear/idempotent). Verified `npm run lint` (clean) + new test file (4 passing). (Completed)

- **Shared Mobile Full-Screen Drawer Host** - Extracted `MobileDrawerHost` + `MobileFullScreenPanel` into `drawer-shared.tsx` (slider + swipe-close drag + grip + scroll/offset reset, with one `sheetCloseRef` the body registers its dirty-guarded close into) so the live-item drawer and the brain-dump draft drawer share a single mobile mount path. `DraftDrawerProvider` now drives BOTH platforms from the store — desktop `DrawerShell` Sheet + mobile `MobileDrawerHost`, both feeding the new shared `DraftEditBody` (the single draft edit body, `fullScreen`-toggled heading + layout); draft cards are render-free (`EditDraftDrawer` only syncs open state into the store via `useLayoutEffect`, with a store subscription on the card resetting `editOpen` on close-from-anywhere). Added `openScrollY` to `DraftDrawerStore` (captured synchronously at the open click before the slider pins the window to 0; restored on close) and dropped the now-unused `onOpenChange` store callback (six→five). Slider restores the page's window scroll as the item slides off (not at idle) to avoid a visible jump on slide-close. Key files `src/components/items/drawer/{drawer-shared,item-detail-drawer,mobile-item-pane-slider}.tsx`, `src/components/parse/parse-draft-card.tsx`, `src/providers/{draft-drawer,item-drawer}-provider.tsx`, `src/stores/draft-drawer-store.ts`. Verified `npm run lint` (clean) + draft-drawer-store tests (5 passing). (Completed)
- **GCP GKE Deployment & Audit** - Audited the entire GCP bootstrap, Terraform configs, Dockerfile builds, GKE manifests, and CI/CD pipelines against Google Cloud and Kubernetes docs, resolving apply/runtime blockers in network policies, Memorystore TLS, storage deletion IAM, Binary Authorization, and Cloud Armor; key files changed under `infra/`, `src/lib/infra/`, `.github/workflows/deploy-gke.yml`, and `Dockerfile` (Completed)
- **Vercel Ignored Build Step** - Added `scripts/vercel-ignore.sh` and `vercel.json` to prevent unnecessary Vercel builds triggered by Dependabot commits or branches matching `chore(deps)` (Completed)
- **Actionlint & GHA Helm Flag Fixes** - Fixed GHA workflow warnings by changing `variables:` to `config-variables:` in `.github/actionlint.yaml`, and resolved the GHA Helm v3-incompatible `--rollback-on-failure` flag by using `--atomic` in `.github/workflows/deploy-gke.yml` while retaining `--rollback-on-failure` for local laptop execution (Helm v4) (Completed)
- **GKE DNS-based Endpoint IAM Condition Fix** - Removed the GKE cluster name IAM condition from the deployer service account's role binding in Terraform to resolve GHA DNS-based endpoint connection authentication failures (HTTP 403) during GKE deployments; key files changed `infra/terraform/modules/iam/main.tf` (Completed)
- **GKE Deployer IAM Role Upgrade to container.admin** - Upgraded the GKE deployer service account's role in Terraform from `roles/container.developer` to `roles/container.admin` to grant permissions required to create and manage cluster-scoped RBAC and Webhook resources during Helm chart deployments; key files changed `infra/terraform/modules/iam/main.tf` (Completed)



- **GKE Server-Side Apply force-conflicts** - Added `--force-conflicts` to both `kubectl apply --server-side` steps in the GHA GKE deploy to perform the one-time client-side→server-side field-ownership transfer, resolving the `ExternalSecret` `.spec.data` conflict with the legacy `kubectl-client-side-apply` manager; verified safe (only declared fields are forced, web Deployment omits `replicas` so HPA is never stomped); key files changed `.github/workflows/deploy-gke.yml`, `infra/docs/09-gcp-audit.md` (Completed)
- **GKE Node Artifact Registry Reader** - Granted the Autopilot node SA (Compute Engine default SA) repo-scoped `roles/artifactregistry.reader` so the kubelet can pull in-project AR images; fixed the migrate Job hanging in `ImagePullBackOff` with a 403 on the pull-token request (the node role grants no AR permissions and the project enforces `iam.automaticIamGrantsForDefaultServiceAccounts`); granted live to unblock immediately, mirrored in Terraform with no drift; key files changed `infra/terraform/modules/iam/main.tf`, `infra/docs/09-gcp-audit.md`, `infra/docs/08-gcp-bootstrap.md` (Completed)
