# P4 — Bugs, Regressions, and Logging — `src/` signals

Apply these alongside the shared signals in `p4.md`. `src/` is the legacy Next.js app
(maintenance-only per `boundary.md`) — audit a change here as you would any live codebase, but the
only `src/`-specific defect signal beyond the shared list is its logger.

- Logging deviates from the stack's logger: `src/` uses Pino via `@/lib/infra/pino`. Audit the
  call shape against `legacy-coding-standards.md § Logging`.
