# The three repo-scoped Artifact Registry IAM member Terraform addresses, in ONE place so the two
# reconcilers that purge them from state on a stranded suspend can never drift on the byte-exact,
# fully-qualified module addresses.
#
# WHY these get stranded: a suspend that ran BEFORE the destroy-order fix (modules/iam
# artifact_registry_repository_depends_on) destroyed the AR repo first, then 403'd trying to remove
# these members via the now-vanished repo — leaving them in state pointing at a repo GCP no longer
# has. The next apply retries the same repo-scoped setIamPolicy and 403s again, re-wedging every
# apply/resume. They cannot be destroyed through the API (no repo to setIamPolicy on), so both
# reconcilers `tofu state rm` them — but ONLY when the repo is genuinely ABSENT in GCP (the exact
# stranded signature); if the repo exists they are legitimately managed and must NOT be touched.
# Recreated on resume (environment_active=true recreates the repo + members).
#
# Consumers (both read this file via devstash_infra.shared.reconcile_ar_iam, skipping blank/`#`
# lines):
#   • devstash-infra gcp apply/resume        (reconcile branch 4, laptop bring-up)
#   • devstash_infra.cloudbuild suspend       (Cloud Build step 4, from the /workspace/repo clone)
# If a member's address changes in modules/iam, edit it HERE and both reconcilers follow.
module.iam.google_artifact_registry_repository_iam_member.node_artifact_registry_reader[0]
module.iam.google_artifact_registry_repository_iam_member.custom_node_artifact_registry_reader[0]
module.iam.google_artifact_registry_repository_iam_member.deployer_artifact_registry[0]
