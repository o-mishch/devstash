node_modules
dist
.output
.tanstack
.nitro
.vite
*.local
.firebase
coverage

# Generated by the TanStack Start/Router plugin on dev/build — never hand-edited.
src/routeTree.gen.ts

# Generated at build time from firebase.template.json by scripts/finalize-dist.ts
# (carries per-build CSP inline-script hashes). Edit the template, not this file.
firebase.json

# Generated at build time by scripts/gen-og-image.tsx (Satori → PNG social card, authored
# in code). No committed image binary — edit the script, not the PNG.
public/og-image.png

# Generated at build time by vite-plugin-sitemap (into dist/, already ignored above).
# Listed for clarity only.
# dist/client/robots.txt
# dist/client/sitemap.xml
